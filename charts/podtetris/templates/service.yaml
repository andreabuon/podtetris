apiVersion: v1
kind: Service
metadata:
  name: {{ include "podtetris.fullname" . }}
  labels:
    {{- include "podtetris.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "podtetris.selectorLabels" . | nindent 4 }}
