apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "podtetris.fullname" . }}-test-connection"
  labels:
    {{- include "podtetris.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "podtetris.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
