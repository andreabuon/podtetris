# Default values for podtetris.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

# Planner CronJob.
# Trigger a run with: `kubectl create job --from=cronjob/<release>-planner <name>`
planner:
  enabled: true
  schedule: "0 0 * * *"
  suspend: true
  image:
    repository: podtetris-planner
    pullPolicy: IfNotPresent
    tag: "latest"
  ttlSecondsAfterFinished: 86400
  activeDeadlineSeconds: 600
  resources:
    limits:
      cpu: 500m
      memory: 256Mi
    requests:
      cpu: 250m
      memory: 128Mi
  config:
    podtetrisNamespace: "{{ .Release.Namespace }}"
    randomCandidateNodesNumber: 3
    byCPUCandidateNodesNumber: 2
    podMoveDefaultCost: 10
    emptyNodesScoreWeight: 400
    costScoreWeight: 1
    autoConsolidationScoreThreshold: 0
    candidateNodesSelectionMaxRetries: 15
    fixedPodAnnotation: "podtetris/fixed"
    enabledPermutationStrategies:
      - cpu_desc
      - memory_desc
      - random
    parallelism: 8
    
  # Pod-move cost rules (first match wins; all set match fields are ANDed).
  costs:
    defaultCost: 10
    rules: []
    # - name: databases
    #   cost: 100
    #   match:
    #     kinds: [StatefulSet]
    #     namespaces: [db]
    #     nameRegex: "^postgres-.*"
    #     labelSelector:
    #       matchLabels:
    #         tier: database

# Evictor controller (PodMove reconciler).
evictor:
  enabled: true
  replicas: 1
  image:
    repository: podtetris-evictor
    pullPolicy: IfNotPresent
    tag: "latest"
  leaderElection: true
  healthProbeBindAddress: ":8081"
  resources:
    limits:
      cpu: 500m
      memory: 256Mi
    requests:
      cpu: 250m
      memory: 128Mi

# Mutating admission webhook (pins replacement pods to PodMove.targetNode).
webhook:
  image:
    repository: podtetris-webhook
    pullPolicy: IfNotPresent
    tag: latest
  replicas: 3
  timeoutSeconds: 5
  failurePolicy: Ignore # 'Ignore', not 'Fail': if the webhook is down, pod creation should not be blocked cluster-wide.
  namespaceSelector:
    matchExpressions:
      - key: kubernetes.io/metadata.name
        operator: NotIn
        values:
          - "{{ .Release.Namespace }}"
          - kube-system
  resources:
    limits:
      cpu: 500m
      memory: 256Mi
    requests:
      cpu: 250m
      memory: 128Mi

# This is for the secrets for pulling an image from a private repository more information can be found here: https://kubernetes.io/docs/tasks/configure-pod-container/pull-image-private-registry/
imagePullSecrets: []
# This is to override the chart name.
nameOverride: ""
fullnameOverride: ""

# This section builds out the service account more information can be found here: https://kubernetes.io/docs/concepts/security/service-accounts/
serviceAccount:
  # Specifies whether a service account should be created.
  create: true
  # Automatically mount a ServiceAccount's API credentials?
  automount: true
  # Annotations to add to the service account.
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template.
  name: ""

rbac:
  create: true

# This is for setting Kubernetes Annotations to a Pod.
# For more information checkout: https://kubernetes.io/docs/concepts/overview/working-with-objects/annotations/
podAnnotations: {}
# This is for setting Kubernetes Labels to a Pod.
# For more information checkout: https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
podLabels: {}

podSecurityContext: {}
  # fsGroup: 2000

securityContext: {}
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
  # runAsNonRoot: true
  # runAsUser: 1000

# Additional volumes (on the planner CronJob and evictor Deployment).
volumes: []
  # - name: foo
  #   secret:
  #     secretName: mysecret
  #     optional: false

# Additional volumeMounts (on the planner CronJob and evictor Deployment).
volumeMounts: []
  # - name: foo
  #   mountPath: "/etc/foo"
  #   readOnly: true

nodeSelector: {}

tolerations: []

affinity: {}

# cert-manager configuration
certificate:
  issuerGroup: "certmanager.step.sm"
  issuerKind: "StepClusterIssuer"
  issuerName: "step-issuer"
  duration: 2160h   # 90 days
  renewBefore: 360h # 15 days
